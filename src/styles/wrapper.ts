import { ViewStyle } from 'react-native';

// 많이 쓸 것 같은 flex wrapper들
export const flexRow = {
  flexDirection: 'row',
  height: '100%',
} as ViewStyle;

export const flexCol = {
  flexDirection: 'column',
  width: '100%',
} as ViewStyle;
