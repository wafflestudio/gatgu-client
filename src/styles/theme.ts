// 색과 font 정보는 여기서 정의

const theme = {
  palette: {
    dark: '#000',
    gray: 'gray',
    white: 'white',
  },
  typo: {
    semiTitle: {
      lineHeight: 30,
      fontSize: 20,
    },
    description: {
      lineHeight: 20,
      fontSize: 15,
    },
  },
};

export const { palette, typo } = theme;
