// App theme
import palette from './palette';

const theme = {
  theme1: {
    // app theme 1: example, TODO remove
    header: palette.fill.dark,
    mainNavigation: palette.fill.red,
  },
  theme2: {
    // 비슷하게
  }
};

export default theme;
